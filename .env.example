# Prisma — SQLite database path (relative to project root)
DATABASE_URL="file:./dev.db"

# Anthropic — required for AI theme classification on new feedback items
# Leave empty to skip classification (items will have ai_theme = null)
ANTHROPIC_API_KEY=
